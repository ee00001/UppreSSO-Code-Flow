import { Codec } from './Codec.js';
export class OprfBaseImpl {
    constructor(mode, suite, crypto, group) {
        this.mode = mode;
        this.suite = suite;
        this.crypto = crypto;
        this.group = group;
        this.codec = new Codec(this.group, this.crypto);
    }
}
//# sourceMappingURL=OprfBaseImpl.js.map